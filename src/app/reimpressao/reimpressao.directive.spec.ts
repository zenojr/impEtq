import { ReimpressaoDirective } from './reimpressao.directive';

describe('ReimpressaoDirective', () => {
  it('should create an instance', () => {
    const directive = new ReimpressaoDirective();
    expect(directive).toBeTruthy();
  });
});
